module.exports = app => {

    // listar
    const list = (req, res, next) => {
    }

    // salvar
    const save = (req, res, next) => {
    }   

    // atualizar
    const update = (req, res, next) => {
    } 
    
    // exibir
    const show = (req, res, next) => {
    } 

    // excluir
    const remove = (req, res, next) => {
    } 

    return { list, save, update, show, remove }
}